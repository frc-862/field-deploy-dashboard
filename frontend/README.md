# Frontend

Vite + React frontend for Field Deploy Dashboard.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

The dev server proxies `/snapshot` to `http://localhost:3000/snapshot` by default (see `vite.config.ts`).
